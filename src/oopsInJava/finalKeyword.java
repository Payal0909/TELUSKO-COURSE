package oopsInJava;

public class finalKeyword {
    public static void main(String[] args) {

     // final keyword is used with variable to make it constant ,that is the value can't be changed later

        // when a class is made final then it means no class can extend that final class and
        // inheritance is stopped


//        when a method is made final it means that no one can override it
    }
}
